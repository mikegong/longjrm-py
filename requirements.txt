# Database drivers (imported conditionally based on database type)
PyMySQL>=1.1.0,<2.0.0
psycopg2-binary>=2.9.0,<3.0.0
pymongo>=4.6.0,<5.0.0

DBUtils>=3.0.3,<4.0.0

# Connection pooling backends
# SQLAlchemy>=2.0.0,<3.0.0  # Optional: Advanced connection pooling (install via: pip install longjrm[sqlalchemy])
