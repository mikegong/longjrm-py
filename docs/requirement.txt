To build a direct adapter between application and database via JSON data instead of OOP object, and get rid off application data model, instead, utilize database model as the only data source, generate required data structure through database engine as possible as what the engine can do. The idea is the database engine is optimized in data computation, and outperforms most of applications. This type of programming paradim can reduce development cost tremendously.
